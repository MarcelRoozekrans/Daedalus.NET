# Phase 2.3 — The Manufacturing Squad

**Date:** 2026-09-23
**Surface:** Backend
**Milestone:** 2 — Software Manufacturing
**Depends on:** 2.2 (durable workflow engine, Thalos.NET 0.7.0)
**Roadmap entry:** "The manufacturing squad: a role roster, per-role memory scoping so a reviewer
does not inherit the implementer's context, and routing between them"

---

## 1. What this phase is for

Phase 2.2 built a durable pipeline and proved it survives a process kill. It did **not** staff that
pipeline. Every task node in `processes/manufacture.yaml` runs as a single agent, `Daedalus
Architect`, which implements the work and then reviews its own work with full access to its own
reasoning.

Phase 2.2's close-out states the consequence plainly: reaching `Succeeded` proves each node called
the outcome tool its schema demanded, not that the work was real. The successful run's `review`
approval almost certainly came from a documented fallback rather than a reading of `implement`'s
output.

This phase makes review **independent and evidenced**. Independent, so the reviewer cannot read the
implementer's reasoning or edit the code it judges. Evidenced, so an approval that did not look at
the work is visibly hollow in the database rather than indistinguishable from a real one.

It does not make a model diligent. See §12.

## 2. Decisions

| # | Decision | Rationale |
|---|---|---|
| D1 | Handoff travels in `WorkflowRun.Variables`; memory is for learning **across** runs | Variables are transactional and need no embedding index. 2.2 observed memory handoff failing silently. |
| D2 | A role **is** an agent — no `Role` entity, no migration | `AgentMemory.AgentId` and the 2.2 name resolver already exist. YAGNI. |
| D3 | Squad disabled falls back to `Daedalus Architect` for every node, and the run says so | A true rollback keeps the pipeline working; 2.2's proof remains the fallback path. |
| D4 | Keep Thalos's existing three-partition `MemoryScope`; the phase's memory work is giving workflow turns a **stable owner** so it functions | The model already exists and is correct. The defect is that the workflow owner is per-run, so nothing accumulates and the roles share a partition. See §5.2. |
| D5 | Tool-boundary enforcement is **retained and asserted**, not built | `MemoryTools` already exposes no agent or scope parameter; identity comes from `MemoryTools.Caller`. The phase adds a test that pins this, so a future parameter cannot reopen it. |
| D6 | Roster is `implementer` + `reviewer`; the Architect covers `adjudicate` and `publish` | 2.2 ruled publishing cannot open a real PR, so a `publisher` role would have no permitted capability. |
| D7 | Recall degrades through tiers and records which tier answered | 2.2's silent-empty recall read as "nothing to know". |
| D8 | The reviewer never receives the implementer's narrative | A reviewer reading the account reviews the account. |
| D9 | The review rubric ships as a versioned skill, not as agent instructions | Improving review becomes editing a document, not a redeploy. `ISkillStore` exists since 1.3. |
| D10 | Outcomes carry evidence; approval states what was checked | Makes a hollow approval visible at the time it happens. |
| D11 | Three review lenses, sequential, short-circuiting on first rejection | Adversarial spend concentrates on approvals, which are the ones that ship. |
| D12 | The reviewer runs a **peer-strength** model from a different family | Uncorrelated blind spots. A same-model reviewer shares the implementer's failure modes. |
| D13 | Only a human-facing interactive agent may write a shared (`AgentId = null`) memory | One bad unattended turn otherwise poisons every role's recall permanently. |

## 3. The roster

Two new agents in `Thalos:Agents` alongside the existing three.

| Node | Squad on | Squad off |
|---|---|---|
| `implement` | `implementer` | `Daedalus Architect` |
| `review` | `reviewer` | `Daedalus Architect` |
| `adjudicate` | `Daedalus Architect` | `Daedalus Architect` |
| `publish` | `Daedalus Architect` | `Daedalus Architect` |

**`implementer`** — `Tools: roslyn__*, daedalus__*, memory__*, skills__*, context7__*`.
Model: default. Holds `roslyn__apply_*`. Holds no `git__*` and no
`repoaction__*`; both remain bound to `developer` in `ToolPolicies`, and the `workflow` principal
does not carry that role.

**`reviewer`** — `Tools: roslyn__find_*, roslyn__get_*, roslyn__analyze_*, roslyn__go_to_definition,
roslyn__list_solutions, roslyn__search_symbols, daedalus__*, memory__*, skills__*, context7__*`.
Model: peer-strength, different family from the implementer's.

**Second correction, 2026-09-23.** The list above originally stopped at `go_to_definition`, and the
shipped configuration does not: `1ee970c` added `roslyn__list_solutions` and `roslyn__search_symbols`
after checking the real `RoslynCodeLens.Mcp` surface, and a test now *requires* both. They are read
tools outside the four prefixes — a reviewer that cannot search symbols cannot find a change whose
`files_touched` pointer is missing or wrong, which is exactly the case the review skill sends it to
`search_symbols` for. Admitting them does not widen the write envelope, which rests on
`apply_code_action` being absent rather than on the list being short. The paragraph above was
corrected in place for the `roslyn__rename_*` error and this one was left, so §3 described the
shipped configuration as a leak it is not.

**Correction, 2026-09-23.** An earlier draft of this section listed `roslyn__rename_*` alongside
`roslyn__apply_*` as a write capability the implementer holds and the reviewer is denied. Task B2
checked the claim against the real tool surface: `RoslynCodeLens.Mcp` registers 32 tools and **no
`rename_*` tool exists**. The `roslyn__rename_*` → `developer` binding in both hosts therefore
matches nothing and protects nothing today. The binding is kept — it is correct if such a tool ever
ships — but it must not be described as an active control, and `roslyn__apply_code_action` is the
sole write tool actually being withheld. Citing a policy that protects nothing as protection is the
defect §6.3's Mechanism lens exists to catch, and it was in this document.

The reviewer's tool list is an explicit read-only enumeration rather than `roslyn__*` minus
exceptions. A reviewer that can edit the code it judges can make its own verdict true, and no
instruction prevents that — only the absent tool does. Enumerating positively means a newly added
`roslyn__apply_something` is absent by default rather than admitted by a wildcard.

### Why not four roles

A `publisher` role would hold no capability it is permitted to exercise: 2.2 ruled Option D, and
publishing a real pull request is deferred to Option C, where host code calls `IPullRequestPublisher`
outside the model's trust path. `adjudicate` is the escape hatch when the visit cap trips — a
judgment call, closest to what the Architect already is. Both earn roles when they earn capabilities.

## 4. The handoff contract

```
implement  writes  { summary, files_touched, rationale }
review     reads   { work_intent, files_touched }
           writes  { verdict, findings[], checked[] }
```

`work_intent` comes from the run's opening variables — what was asked — not from the implementer.
`files_touched` is a **pointer**: the reviewer reads the actual code through `roslyn__*` and judges
delivered against asked.

**`work_intent` has no producer in `src` today**, because nothing in `src` starts a run — see §12.
The contract is written for the surface phase 2.4 owes it; what a review dispatch can actually carry
in production right now is `files_touched` alone.

`summary` and `rationale` are written by the implementer and are **not** passed to the reviewer.
They remain in the run record for humans and for `adjudicate`. Withholding `rationale` while passing
`summary` would be a half-measure: a summary is rationale in compressed form, and a reviewer reading
it evaluates the argument rather than the artifact.

This is the isolation that matters most, because unlike the memory tier it is inspectable after the
fact — the variables are a database row.

**Where the withholding actually happens, established in task B5.** Thalos 0.9.0's
`WorkflowNodeDispatcher.BuildTaskText` renders the **whole** of `WorkflowRun.Variables` into every
node's task text. A projection applied downstream of the dispatcher — which is where task B4 put
one, correctly for 0.8.0, where nothing populated the bag at all — therefore withholds nothing the
moment the bag is real. The projection runs between the store and the dispatcher instead:
`ReviewHandoffWorkflowStore` narrows `IWorkflowStore.FindAsync`'s result to the review contract's
read keys for a node that declares `lenses:`, so the withheld values never reach the component that
renders them. The mechanism is absence, not filtering; there is no rendered block to strip and no
delimiter whose change could silently reopen it. Only the dispatcher's copy of the store is
decorated, so the run itself, and anyone reading it, still holds all three keys.

### 4.1 The implement step becomes real — a reversal of a phase 2.2 boundary

`skills/manufacture-implement/SKILL.md` as shipped by phase 2.2 is titled **"Implement step
(read-only)"** and its frontmatter says *"without touching the repository"*. It produces a
*description* of a change and makes none. That was deliberate: 2.2 proved the engine, and an
unattended agent editing code was out of scope.

This section's handoff contract only makes sense if `implement` actually edits files — otherwise
`files_touched` points at nothing and the reviewer reads prose, which is the half-measure §4 exists
to forbid. The pipeline would reach `Succeeded` with zero code change: this phase's own defect,
relocated from `review` to `implement`.

**Decision, taken by the project owner on 2026-09-23: make `implement` real.** Task B4 rewrites
the skill so the implementer applies changes through `roslyn__apply_code_action`, and the reviewer
reads the resulting code. This knowingly reverses 2.2's read-only boundary.

**Correction, 2026-09-23, after task B5 checked the tool rather than the intention.**
`roslyn__apply_code_action` is **not arbitrary editing**. Its own schema: *"Apply a code action
(refactoring or fix) **by its title**. Use `get_code_actions` first to discover available actions.
**Defaults to preview mode** — returns diff without writing files. Set `preview=false` to apply
changes to disk."*

So the implementer can apply a refactoring or fix **that Roslyn already offers at a position**, and
only writes when it explicitly passes `preview=false`. It cannot write new code, add a method with
an arbitrary body, create a file, or make any change Roslyn does not offer.

The decision recorded above was taken on the framing that the implementer "edits the working tree".
That framing was mine and it was too broad: the real capability is a fraction of it. What §4's
handoff contract can carry is therefore narrower than it reads — `files_touched` names files a
Roslyn-offered action changed, not files an agent authored. Widening that is a capability decision
in its own right, and it is not taken here.

**The write envelope, which does not widen:** the implementer holds `roslyn__apply_*` and **no
`git__*` and no `repoaction__*`**. It can edit files. It cannot branch, commit, push, or open a
pull request. That is the same boundary phase 2.1 built and 2.2 defended, and it is what makes this
decision bounded rather than open-ended.

**What a run leaves behind.** Nothing reverts an implement round's edits. A rejected review, a cap
redirect to `adjudicate`, a failed run and a stranded run all leave the working tree modified, and
with `maxVisits: 5` up to five rounds of edits can accumulate on top of each other. The implementer
cannot isolate its work on a branch, because it has no git tools. **A run is therefore not a no-op
when it fails**, and deciding what to do with the resulting changes is a human step — the same
shape as publishing being human-triggered.

## 5. Memory scoping

### 5.1 What already exists — verified against the package, not assumed

`Thalos.NET.Memory` 0.7.0 already implements the visibility model this phase needs. `MemoryScope` is
documented as **"the single visibility rule"**, and `MemoryScope.Partitions` reads three:

| Partition | Contents |
|---|---|
| `(owner, agent)` | memories pinned to the calling agent |
| `(owner, null)` | the caller's own owner-wide memories |
| `(sharedOwner, null)` | the host's shared memories, when configured |

`MemoryTools.RecallAsync` and `ListAsync` expose **no** agent or scope parameter. The calling identity
comes from `MemoryTools.Caller` — "the turn's owner and agent". Enforcement is therefore already at
the tool boundary, which is what D5 asks for. Daedalus sets `SharedOwnerId = "daedalus"`, so the
shared partition is live rather than inert.

**An earlier draft of this design claimed a gap here and was wrong.** It read
`PostgresMemoryStore`'s `q.Where(m => m.AgentId == agentGuid)` and concluded there was no
"own plus shared" query. That filter is a query *primitive* implementing `MemoryQuery.AgentId`,
documented as "only records pinned to this agent"; `MemoryScope` composes the three partitions above
from it. The filter behaves exactly as specified. The error was reading a low-level primitive and
inferring a missing capability without checking its caller — the same defect class §6.3's Mechanism
lens exists to catch.

### 5.2 The real defect: the workflow memory owner is per-run

```csharp
// WorkflowCaller.cs
public string Id { get; } = $"workflow:{run.Process}:{run.Id}";
```

The owner identity contains the run id, so it changes with every run. Two consequences:

**Per-role learning cannot accumulate.** A memory written by `reviewer` during run A is stored under
owner `workflow:manufacture:A`. Run B's caller is `workflow:manufacture:B` — a different owner,
outside that scope, permanently. Only the `daedalus` shared partition survives a run boundary. The
premise of per-role memory, that a reviewer becomes better at this project over time, does not hold
today and cannot without this change.

**Within a single run the roles share an owner.** `implementer` and `reviewer` both run under
`workflow:manufacture:{runId}`, so the `(owner, null)` partition is common to both. Any memory not
pinned to an agent is mutually visible — a hole in the isolation this phase exists to create.

This, rather than the scoping model, is the memory work of phase 2.3.

### 5.3 The fix, and why it is not a one-line edit

Workflow turns get a **stable memory owner**, with the agent pin carrying the role separation.

**Corrected after implementation, 2026-09-23.** An earlier version of this table named the owner
`daedalus` for all three rows. Every behavioural claim it made holds; the identifiers did not. The
memory owner a workflow turn runs under is `WorkflowCaller.MemoryOwnerId`, which is
`workflow:{process}` — `workflow:manufacture` for this pipeline — and is deliberately *not* the host's
shared owner. `daedalus` is `Thalos:Memory:SharedOwnerId`, a fourth, separate partition every caller
on the host reads. The row that matters most was missing entirely:

| Partition | Contents | Who reads it |
|---|---|---|
| `(workflow:manufacture, reviewer)` | the reviewer's own learning, surviving across runs | reviewer only |
| `(workflow:manufacture, implementer)` | the implementer's own learning | implementer only |
| `(workflow:manufacture, null)` | owner-wide memories of this process | **both roles, by default** |
| `(daedalus, null)` | the host's shared project facts | every caller on the host |

The third row is the hole this section exists to close, and nothing in `MemoryScope` closes it —
`Partitions` reads `(owner, null)` for every caller of that owner, so a memory landing there is
mutually visible. What closes it is `WorkflowCaller.PinMemoriesToAgent => true`, which makes
`MemoryTools.RememberAsync` pin every memory a workflow turn writes to that turn's agent and ignore
the tool's own `shared` parameter. So the separation is on the **write** side, not the read side,
and the third row stays empty for workflow turns rather than being filtered out of their reads.

`WorkflowCaller.Id` **must not** simply be changed to achieve this. That property is
`ISecurityContext.Id` — the authorization identity `DefaultToolAuthorizer` evaluates and the value
that makes a run auditable. It stays per-run. The memory owner is therefore derived separately from
the security principal: the caller exposes a stable memory-owner identity alongside its per-run
authorization identity, and the two are deliberately different.

Whether a workflow agent can write to the shared partition at all is the subject of D13 and must be
verified against `MemoryTools.RememberAsync`'s parameters during implementation — if an agent can
opt into a shared write, that path is closed for unattended roles.

### 5.4 Tiered recall

Recall degrades rather than returning empty, and records which tier answered:

| Tier | Source | Used when |
|---|---|---|
| 1 | Semantic search over the Rag.NET index | index warm, embedded rows exist |
| 2 | Recency-ordered rows direct from Postgres, within the caller's `MemoryScope` | index cold or unavailable |
| 3 | None — the agent proceeds on instructions and skill alone | no rows in scope |

Phase 2.2 observed every `AgentMemories` row stuck at `IndexPending = true` for a run's lifetime, so
semantic recall had nothing to search and returned silence; the agent found nothing wrong and
proceeded. `ReindexPendingMemoriesHostedService` and Ollama with `nomic-embed-text` both exist and
are wired in `AppHost` — the pipeline is not missing, it was not warm inside the run's lifetime.

Note that §5.2 is a second, independent reason 2.2's recall found nothing: even a warm index would
have searched an owner partition that no earlier run had ever written to.

The tier is written to the run record. **Where exactly, corrected in task B5 after checking what the
packages allow:** Thalos records nothing per *turn* that a host can add to — `MemoryContextProvider`
publishes `MemoryRecalledEvent` without the tier and publishes nothing at all when recall came back
empty, which is precisely the case this section is about. The only host-writable slot in a run's
record is `NodeResult.Variables`, which `OrmWorkflowStore` writes, unmerged, into that transition's
`workflow_run_event` row. So the tier lands on the **node's transition**, under `recall_tier`, and
for a node that takes several turns — a `review` node running three lens passes — it is the tier
that answered the node's *last* turn. A node whose turn made no recall records no `recall_tier` at
all, which is deliberately a different statement from tier `None`. "Tier 2, index cold" is
auditable; silence is not.

## 6. Review quality

### 6.1 The rubric is a skill

The reviewer loads a versioned skill through `ISkillStore` rather than carrying its criteria in
`Instructions`. Changing how review works then means editing a document under `skills/`, which is
also the direction phase 2.4 takes for the process as a whole.

### 6.2 Outcomes carry evidence

The `review` node's outcome schema, extending 2.2's closed-set mechanism:

- **reject** requires `findings[]`, each with `file`, `line` and a failure scenario.
- **approve** requires `checked[]` — what was examined and found sound.

An approval with an empty `checked[]` is visibly hollow **in the database**, at the moment it
happens. Phase 2.2's `Succeeded` run is the motivating case: its approval was indistinguishable from
a real one until a human reasoned about it afterwards.

### 6.3 Three lenses, sequential, short-circuiting

The `review` node runs up to three passes, one lens each, stopping at the first rejection:

| Lens | Asks | Defect class it targets |
|---|---|---|
| Correctness | Does it do what was asked; name the input that breaks it | 2.2's `StartAsync` never enqueuing the first dispatch |
| Falsifiability | For each test and control added, what turns red if removed | four unguarded fixes in 2.2 Part B; two non-failing guards in 1.7 |
| Mechanism | For each claim in a comment, doc or name — does the thing it credits enforce it | nine wrong-mechanism instances in 2.2 |

Each lens is chosen because it would have caught defects this project actually shipped. A rejection
stops the pipeline already, so confirming it with two more passes buys nothing; an **approval must
survive all three**. Cost is therefore proportional to quality: wrong work costs one lens, right work
costs three.

This needs **no engine change**. Phase 2.2's invariant is one *transition* per transaction, not one
*turn* per node — a node may take several turns and complete once. This activates the `lenses` and
`quorum` fields that 2.2 parsed and left inert.

Worst case is `maxVisits: 5` × 3 lenses = 15 review turns; the realistic path is one or two
iterations. Every turn is bounded by 2.2's per-turn budget decorator.

## 7. The squad flag

`Thalos:Squad:Enabled`, defaulting to the same pattern as `Thalos:Workflow:Enabled`.

Disabled maps **every** node to `Daedalus Architect` at resolution time. The process file is
unchanged, so `manufacture.yaml` needs no edit to roll back and 2.2's end-to-end proof remains a
working configuration rather than only history.

**The fallback must be loud.** With the squad off, one agent implements and reviews its own work.
That fact is written to `workflow_run_event` on the transition. Otherwise a `Succeeded` run under a
disabled squad is indistinguishable from one with genuine independent review — the exact class of
false assurance this phase exists to remove.

**Refined in task B5: every transition, not just the first.** A host decorator has no cheap,
reliable signal for "this is the run's first transition", and more importantly a run parked at an
approval gate for days can resume onto a host whose `Thalos:Squad:Enabled` has changed since. A
record that stamped only the first transition would claim a mode half the run never executed in, so
each event states the mode that produced *that* transition. The gate's own resume event carries no
mode: `IWorkflowStore.ResumeAsync` takes a payload string rather than a variable bag, and a human's
signal is not an agent turn to describe.

## 8. Models and cost

Per-agent model selection already exists: `AgentOptions.Model` ("provider model id; null →
`Thalos:Anthropic:DefaultModel`") and `AgentOptions.MaxOutputTokens`. Assigning the reviewer a
different model is configuration, not code.

The reviewer runs a **peer-strength** model from a different family (D12). The motivation is
independence rather than thrift: a reviewer sharing the implementer's model shares its blind spots
and will tend to accept exactly the reasoning errors that model is prone to making.

**Cost control comes from the visit cap and the budget decorator, both from 2.2 — and the budget is
not currently sized for this workload.** `DetachedRuns:MaxTotalTokens` is `150000` on both hosts.
One measured `implement` turn during this phase used **358703 input tokens**, so on that evidence
the budget decorator does not bound an implement turn, it *fails* it: a guaranteed
`SubagentBudgetExceeded` on the first one. The number is deliberately left where it is rather than
raised to fit the measurement — a ceiling moved to make one observed turn pass is not a sizing, and
what the right sizing is has not been worked out. See §12.

### 8.1 The pricing defect this phase fixes

`CostAnalyticsService` resolves an unpriced model like this:

```csharp
if (!_pricing.Models.TryGetValue(modelId, out var pricing))
{
    var first = _pricing.Models.FirstOrDefault();
    modelId = first.Key ?? modelId;          // relabels the model
    pricing = first.Value ?? new ModelPricing { 3.0m, 15.0m };
}
```

An unpriced model silently borrows the **first dictionary entry** and renames itself to it.
`ModelPricing.Models` lists `claude-sonnet-4-20250514`, `claude-haiku-4-5-20251001` and
`claude-opus-4-20250514`, while `Thalos:Anthropic:DefaultModel` is `claude-sonnet-5` — which is not
in the list. Every run today is costed at Sonnet 4 rates and reported as Sonnet 4.

This phase prices the models actually in use and makes an unpriced model **fail loudly** rather than
impersonate another. A phase that introduces a second model cannot leave cost attribution silently
wrong, because comparing the two models' cost is the first thing anyone will try.

## 9. Error handling

| Condition | Behaviour |
|---|---|
| Squad enabled, a roster agent missing from the catalog | Fails at **load**. 2.2 collapsed `AgentExistsAsync` and `ResolveAgentIdAsync` into one method precisely so a process cannot validate clean and then fail per node on a live, paying run. |
| Semantic index cold or unavailable | Tier 2 recall; tier recorded on the turn. |
| No memories at all | Tier 3; recorded. Not an error. |
| Unpriced model | Loud failure, not silent substitution (§8.1). |
| Reviewer attempts a write tool | Not reachable — `roslyn__apply_*` is absent from its list, and separately bound to `developer` in `ToolPolicies`. Absence is the mechanism that holds here; the policy is the second line. |
| Lens 1 or 2 rejects | Short-circuit; remaining lenses do not run. |
| Reviewer returns an approval with empty `checked[]` | Schema violation — the outcome tool rejects it, the turn does not complete the node. |

## 10. Testing

Per-assertion falsifiability, per `CLAUDE.md`. Each control below names the change that turns it red,
and each must be **verified red by making that change**, not by asserting it would.

| Assertion | Turns red when |
|---|---|
| The recall tool surface exposes no agent/scope parameter | someone adds one back — a schema assertion over the tool definition, pinning what 0.7.0 already gives us |
| A reviewer recalling against a DB seeded with implementer-pinned rows receives its own plus shared only, never the implementer's | `MemoryScope`'s partition set widens, or the workflow caller stops pinning to the agent |
| The `review` node's variables contain `files_touched` and `work_intent` and **not** `summary` or `rationale` | the contract leaks either field |
| The reviewer's tool list contains no `roslyn__apply_*` | a wildcard replaces the positive enumeration |
| With the squad disabled, every node resolves to `Daedalus Architect` **and** the run event records the mode | the fallback stops being recorded |
| An approval with empty `checked[]` is rejected by the outcome tool | the schema stops requiring evidence |
| An unpriced model fails loudly | the silent first-entry substitution returns |
| A memory written by `reviewer` in one run is recallable by `reviewer` in the **next** run | the workflow memory owner goes back to being per-run |
| A memory written by `implementer` is **not** recallable by `reviewer` in the same run | the roles share an owner partition and nothing pins to the agent |
| Lens 2 and 3 do not run after lens 1 rejects | short-circuiting is lost and cost silently triples |

The seeded-database requirement on the second row is load-bearing: a scoping test over an empty
store passes whatever the filter says. That is the vacuous shape this codebase keeps producing —
phase 1.7 alone found eight instances.

## 11. Out of scope

- **Parallel agent dispatch.** Sequential lenses give panel review without it. True parallelism
  breaks 2.2's single-transition-per-transaction invariant and is not needed for this phase's goal.
- **Charter-as-markdown.** Agents stay in configuration. Defining roles as documents belongs to
  phase 2.4, whose stated goal is the process as skills.
- **A shared configuration repository.** Parked with reasoning in `docs/planning/parked-ideas.md`;
  revisit after 2.4. It turns process definitions into a supply chain and needs provenance designed,
  not discovered.
- **A `publisher` role.** Earns its place when Option C gives it a capability (§3).
- **Fixing semantic indexing latency.** Tiered recall makes a cold index survivable rather than
  fatal. Making it warm faster is a separate concern.

## 12. Honest limits

**This phase does not prove the reviewer reviews well.** It proves the reviewer cannot read the
implementer's reasoning, cannot edit the code it judges, must produce evidence to approve, and must
survive three lenses to do so. Those remove the mechanisms by which a hollow approval could pass
*quietly*. A determined model can still produce a plausible `checked[]` for work it skimmed.

What changes is that such an approval is now **visible in the database at the time it happens** —
`checked[]` naming things that do not exist, or three lens passes that raise nothing against a real
defect — rather than reconstructed from a run that merely said `Succeeded`.

**The squad-disabled path is genuinely weaker**, by design, and says so in the run record.

**The per-turn budget is not sized for an implement turn, and this phase does not resize it.**
`DetachedRuns:MaxTotalTokens` is `150000` on both hosts; one measured implement turn used **358703
input tokens**. §8 describes the budget decorator as a cost control, and on that measurement it is
not one — it is a guaranteed `SubagentBudgetExceeded` on the first implement turn. Raising the
number to fit one observation would be picking a value rather than sizing one, so the measurement is
recorded and the sizing is left open. The comment above the key in `appsettings.json` explains the
*scout's* sizing, from an earlier phase, and says nothing about manufacturing.

**`work_intent` has no producer, so the reviewer receives one of its two inputs, not two.** §4's
contract says `work_intent` comes from the run's opening variables, and
`skills/manufacture-review/SKILL.md` tells the reviewer it receives it. Nothing in `src` starts a
workflow run: the only `StartAsync` reference is `DelegatingWorkflowStore`'s pass-through, and phase
2.2's own close-out already records that no "start a run" surface exists in Daedalus — its live
proof called `OrmWorkflowStore.StartAsync` from a throwaway console harness. So in production today
the only key a review dispatch can actually carry is `files_touched`. `SquadHandoffEndToEndTests`
seeds `work_intent` by calling `StartAsync` itself, which is why the contract is testable and still
has no production path. This is phase 2.2 carried-forward item 2, and it is now load-bearing for
this phase rather than merely inconvenient.

**The three lenses are chosen from this repository's defect history.** They are well-aimed at the
mistakes this project has made, and correspondingly less aimed at mistakes it has not made yet.
